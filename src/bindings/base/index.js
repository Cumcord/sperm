const cumcord = {
  modules: window.cumcord.modules,
  patcher: window.cumcord.patcher,
};

export default cumcord;

export const modules = cumcord.modules;
export const patcher = cumcord.patcher;
